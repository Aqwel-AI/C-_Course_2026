# Homework 09: Templates

1. Write a template `minValue` function.
2. Write a template `swapValue` function.
3. Create a `Pair<T, U>` struct template.
4. Add specialization of `Pair` for `std::string`.
5. Write a template function to sum a vector.
6. Implement a generic `Stack<T>`.
7. Bonus: Use C++20 concepts to restrict a function to arithmetic types.
