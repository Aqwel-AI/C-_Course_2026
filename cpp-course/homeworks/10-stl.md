# Homework 10: STL

1. Store 10 integers in a `vector` and print them.
2. Use `set` to remove duplicates from input.
3. Build a word frequency counter with `map`.
4. Sort a vector with a custom comparator.
5. Use `unordered_map` for fast lookups.
6. Use `find_if` with a lambda to find a value.
7. Bonus: Implement a simple LRU cache.
