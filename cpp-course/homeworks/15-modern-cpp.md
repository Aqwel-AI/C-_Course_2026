# Homework 15: Modern C++

1. Replace a classic loop with a range-based loop.
2. Use `auto` for iterator declarations.
3. Create and use an `enum class`.
4. Write a `constexpr` function for factorial.
5. Demonstrate move semantics with a custom class.
6. Use `std::optional` to represent missing values.
7. Bonus: Use `std::variant` for a small type-safe union.
