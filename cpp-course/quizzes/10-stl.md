# Quiz 10: STL

1. What is an iterator?
2. When would you use `vector` vs `list`?
3. What is the complexity of `unordered_map` lookup on average?
4. What is a lambda function?
5. Why are STL algorithms reusable?
