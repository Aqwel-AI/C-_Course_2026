# Quiz 03: Control Flow

1. When should you use a `while` loop?
2. What does `break` do inside a loop?
3. Why is `switch` sometimes faster than `if-else` chains?
4. What is an off-by-one error?
5. What is the difference between `for` and `do-while`?
