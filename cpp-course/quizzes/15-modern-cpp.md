# Quiz 15: Modern C++

1. What does `auto` do?
2. What is move semantics?
3. What is the difference between `nullptr` and `0`?
4. What does `constexpr` mean?
5. Why use `enum class` instead of `enum`?
